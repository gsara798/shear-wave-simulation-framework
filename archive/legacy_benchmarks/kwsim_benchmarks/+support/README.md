# Benchmark support

Internal helpers shared by benchmark packages.

This package contains execution, evaluation, and visualization mechanics that
are common to the directional, partially diffuse, and diffuse regime suites.

It is not part of the reusable `kwsim` library API. Scientific configurations,
benchmark identities, and acceptance entry points remain in their respective
benchmark packages.
