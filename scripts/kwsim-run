#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIRECTORY="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Note: scripts/kwsim-run is a compatibility wrapper; use scripts/sim-run for new workflows." >&2
exec bash "$SCRIPT_DIRECTORY/sim-run" "$@"
